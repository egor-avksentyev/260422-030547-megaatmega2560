# Коды ИК-пультов

Считаны через `IRremote` (`IrReceiver.printIRResultShort()`), приёмник на `IR_PIN` (пин 19).
Используются в `src/hardware_settings.h` (`IR_RIGHT`/`IR_LEFT`/`IR_ENTER`/`IR_MUTE`/`IR_POWER`/`IR_UP`/`IR_DOWN`)
и разбираются в `src/remote_control.cpp` (`handleRemoteInput()`).

Right/Left — только навигация по меню (без моторов). Up/Down — только мотор Bass/High/Volume
в режиме настроек (не переключают пункты меню): держишь — крутится, отпустил — стоп (те же
repeat-кадры пульта и тот же таймаут простоя мотора, что и у энкодера).

## Старые коды (до переобучения) — больше не используются

Пульт был переобучен (см. [[project_ir_remote_training]] — нестабильная работа была
из-за плохого обучения, не из-за прошивки), и раньше слал разные протоколы на разные
кнопки (Samsung/NEC вперемешку). После переобучения все кнопки — единый протокол RC5.
Старые коды не выброшены на случай регрессии, но в `hardware_settings.h` не задействованы.

| Кнопка | Протокол | Address | Command |
|---|---|---|---|
| Power | Samsung | `0x7` | `0x2` |
| Right | Samsung | `0x7` | `0x7` |
| Left | Samsung | `0x7` | `0xB` |
| Enter | NEC | `0x0` | `0x9` |
| Mute | Samsung | `0x7` | `0xF` |
| Up | NEC | `0x5` | `0xEF` |
| Down | NEC | `0x5` | `0xEE` |
| Right (совсем старый) | NEC | — | `0x79` |
| Left (совсем старый) | NEC | — | `0xF9` |
| Enter (совсем старый) | NEC | — | `0x7B` |
| Mute (совсем старый) | NEC | — | `0x38` |

## Текущий пульт (переобучен, RC5, Address=0x18)

| Кнопка | Command | В коде |
|---|---|---|
| Power | `0x1` | `IR_POWER` |
| Mute | `0x2` | `IR_MUTE` |
| Right | `0x4` | `IR_RIGHT` |
| Left | `0x6` | `IR_LEFT` |
| Down | `0x8` | `IR_DOWN` |
| Enter | `0x0` | `IR_ENTER` |
| Up | `0x1D` | `IR_UP` |
| Set | `0x21` | — |
| Menu | `0x20` | — |
| Back | `0x11` | — |
| F1 | `0x2E` | — |
| F2 | `0x36` | — |
| F3 | `0x32` | — |

"—" в колонке "В коде" — код известен, но пока ни на что не назначен в `remote_control.cpp`.
Все кнопки теперь одного протокола/адреса — код в `remote_control.cpp` матчит только
по `Command`, коллизий со старыми значениями не проверяет (они больше не приходят).
