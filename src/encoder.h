#pragma once

// ============================================================================
// encoder.h — квадратурный энкодер (алгоритм Мазурова, читается по прерыванию
// CHANGE на обоих пинах) и его кнопка (одиночный клик — вход/выход из настроек
// пункта меню, двойной клик — выход из настроек).
// ============================================================================

#include <Arduino.h>

// Пишется из encoderISR() по прерыванию — обязательно volatile. Положительное
// значение — вращение в одну сторону, отрицательное — в другую; loop() сбрасывает
// его в 0 после обработки одного шага
extern volatile int encoderValue;

void encoderISR();
void checkEncoderButton();
