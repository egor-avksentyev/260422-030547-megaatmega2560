#pragma once

// ============================================================================
// temperature_sensor.h — три датчика DS18B20 (каждый на своей 1-Wire шине, см.
// TEMP_SENSOR_*_PIN в hardware_settings.h), обслуживают пункт меню "Info".
// ============================================================================

#define TEMP_SENSOR_INVALID -127.0f // Совпадает с DEVICE_DISCONNECTED_C библиотеки DallasTemperature — отдаётся, если датчик не ответил (обрыв/не подключен/конверсия ещё не готова)

void initTemperatureSensors();

// Заполняет out[0..2] последним готовым результатом (или TEMP_SENSOR_INVALID, если датчик
// не отвечает/конверсия ни разу не завершалась) и сразу просит новую конверсию у всех трёх —
// не блокирует, см. подробности в .cpp
void readAllTemperatures(float* out);
