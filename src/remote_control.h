#pragma once

// ============================================================================
// remote_control.h — ИК-пульт: декодирование RC5 через собственный декодер на
// Input Capture Timer4 (rc5_icu.h/.cpp, пин 49 — не через библиотеку IRremote, от
// которой отказались: та таймерным опросом IR_PIN=19 периодически теряла кадры,
// если приём накладывался на NeoPixel.show(), отключающий прерывания на время
// бит-банга колец — см. rc5_icu.h за подробным разбором) и обработка команд
// (IR_RIGHT/LEFT/ENTER/MUTE/POWER/UP/DOWN/SET) в loop(). Дублирует по смыслу часть
// логики энкодера — при изменении поведения пункта меню правь оба места (см. encoder.cpp).
// ============================================================================

void initRemoteControl();
void handleRemoteInput();
