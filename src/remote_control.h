#pragma once

// ============================================================================
// remote_control.h — ИК-пульт(ы): декодирование через IRremote (не NecDecoder —
// та понимает только протокол NEC, а часть кнопок/второй пульт может быть на
// другом протоколе, например Samsung; IRremote распознаёт оба одновременно) и
// обработка команд (IR_RIGHT/LEFT/ENTER/MUTE/POWER/UP/DOWN) в loop(). Дублирует по
// смыслу часть логики энкодера — при изменении поведения пункта меню правь
// оба места (см. encoder.cpp).
// ============================================================================

void initRemoteControl();
void handleRemoteInput();
