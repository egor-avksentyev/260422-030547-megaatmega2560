#pragma once

// ============================================================================
// mute_animation.h — полноэкранная анимация, занимает весь экран НЕПРЕРЫВНО, всё время,
// пока включён Mute (см. remote_control.cpp, case IR_MUTE, и main.cpp, loop()).
// ============================================================================

// Не блокирующая — рисует и шлёт очередной кадр, только если для него настало время
// (см. MUTE_ANIM_FRAME_DELAY_MS), иначе сразу возвращает управление. Вызывать на каждой
// итерации loop(), пока isMuted — крутится сама, пока её продолжают звать
void animateMuteFrame();

// Вызывать один раз в момент включения Mute (case IR_MUTE в remote_control.cpp) — форсирует
// полную перерисовку на СЛЕДУЮЩЕМ вызове animateMuteFrame() (стирает то, что было на экране
// до Mute), все последующие кадры уже частичные (см. animateMuteFrame())
void resetMuteAnimation();
